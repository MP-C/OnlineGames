function litres(time) {
    return Math.floor(time*0.5);
    //litres = Math.round((time*0.5))
    //return litres - (litres %1)
    //return Math.floor(litres)
    //return Math.trunc(litres)
    //return litres%2
    //return Math.round(litres) 
    //return parseInt(litres.toFixed(0))
    //return parseInt(litres)
    //return litres.toFixed(0);
    /*
    for(let i; i<litres.lenght; i++)
        while (i!="."){
    } return litres
    */
}